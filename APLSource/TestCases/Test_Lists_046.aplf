 R←Test_Lists_046(stopFlag batchFlag);⎕TRAP;md;ns;parms;noOfListItems;expected
⍝ Test a list item that contains a code block with additional blanks between list indicator and content.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'This is a para.'
 md,←⊂'*     This'
 md,←⊂'*     That'
 md,←⊂''
 md,←⊂'      ~~~'
 md,←⊂'      {+⌿⍺,⍵}'
 md,←⊂'      ~~~'
 md,←⊂'*     More '

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 expected←'<p>This is a para.</p>' '<ul>' '<li>This</li>' '<li>That' '<pre><code>{+⌿⍺,⍵}</code></pre></li>' '<li>More</li>' '</ul>'
 →PassesIf expected≡ns.html

 R←∆OK
