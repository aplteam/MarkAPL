 R←Test_Para_012(stopFlag batchFlag);⎕TRAP;ns;md;expected
⍝ Test a paragraph code that containes "<p>" and "</p>"; was once  abug.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'A paragraph'
 md,←⊂''
 md,←⊂' <p>Start of a para that contains a commented line.</p>'  ⍝ Leading space to avoid this becming an HTML block.
 md,←⊂''
 md,←⊂'Another paragraph'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 expected←'<p>&lt;p&gt;Start of a para that contains a commented line.&lt;/p&gt;</p>'
 →PassesIf∨/expected⍷∊ns.html

 R←∆OK
