 R←Test_Table_001(stopFlag batchFlag);⎕TRAP;ns;parms;mat;html
⍝ Test table with column headers
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 mat←''
 mat,←⊂'|Name|Remark|No. of users|'
 mat,←⊂'|---|---|---|'
 mat,←⊂'|APL|Brilliant|800|'
 mat,←⊂'|Cobol|Old|14000|'
 mat,←⊂'|F#|New|3400|'

 ns←##.MarkAPL.Init''mat
 ns←##.MarkAPL.Process ns
 html←1↓∊(⎕UCS 13),¨ns.html
 →FailsIf 6≠+/'<td>'⍷html
 →FailsIf 3≠+/'<td style'⍷html
 →FailsIf 9≠+/'</td'⍷html
 →FailsIf 3≠+/'<th '⍷html
 →FailsIf 3≠+/'</th>'⍷html
 →FailsIf 4≠+/'<tr>'⍷html
 →FailsIf 4≠+/'</tr>'⍷html
 →FailsIf 1≠+/'<thead>'⍷html
 →FailsIf 1≠+/'</thead>'⍷html
 →FailsIf 1≠+/'<tbody>'⍷html
 →FailsIf 1≠+/'</tbody>'⍷html
 →FailsIf 1≠+/'<table>'⍷html
 →FailsIf 1≠+/'</table>'⍷html

 R←∆OK
