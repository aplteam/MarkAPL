 R←Test_Table_027(stopFlag batchFlag);⎕TRAP;ns;mat;expected
⍝ Test table with a left-most cell that has an odd number of ticks.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 mat←''
 mat,←⊂'|`APL|Powerful|'
 mat,←⊂'|COBOL|Outdated|'

 ns←##.MarkAPL.Init''mat
 ns←##.MarkAPL.Process ns
 expected←'<table>' '<tbody>' '<tr>' '<td style="text-align: left;"><code>APL|Powerful|</code></td>'
 expected,←'<td style="text-align: left;">                          </td>' '</tr>' '<tr>' '<td style="text-align: left;">COBOL</td>'
 expected,←⊂'<td style="text-align: left;">Outdated</td>'
 expected,←'</tr>' '</tbody>' '</table>'
 →T.PassesIf expected≡ns.html

 R←T._OK
