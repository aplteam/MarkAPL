 R←Test_CompressCSS_003(stopFlag batchFlag);⎕TRAP;css;css2
⍝ Try to compress CSS with invalid comment definition; must fail
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 css←''
 css,←⊂'/*multi'
 css,←⊂'/*line'      ⍝ That's wrong!
 css,←⊂'comment */'
 css,←⊂''
 css,←⊂'p {'
 css,←⊂'  font-size : 19pt;'
 css,←⊂'}'

 :Trap 11
     css2←##.MarkAPL.CompressCSS css
     →FailsIf 1
 :Else
     →PassesIf'Cannot compress CSS:'{⍺≡(⍴⍺)↑⍵}↑⎕DM
 :EndTrap

 R←∆OK
