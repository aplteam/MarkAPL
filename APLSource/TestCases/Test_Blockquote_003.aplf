 R←Test_Blockquote_003(stopFlag batchFlag);⎕TRAP;md;ns;parms
⍝ Test a blockquotes that has a para spanning more than one line.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 md←''
 md,←⊂'> This is a para.'
 md,←⊂'Still the same blockquote.'
 md,←⊂'Keeps going.'
 md,←⊂''
 md,←⊂'New --- non-blockquote -- para.'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 →T.PassesIf'<blockquote>'≡0⊃ns.html
 →T.PassesIf'<p>This is a para. Still the same blockquote. Keeps going.</p>'≡1⊃ns.html

 R←T._OK
