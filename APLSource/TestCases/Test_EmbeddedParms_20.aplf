 R←Test_EmbeddedParms_20(stopFlag batchFlag);⎕TRAP;md;b;ns;html;hits;filenames;path;parms
⍝ Check that a parameter definition ends after two consecutive lines
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'[parm]:cssURL      = ''Firlefanz'''
 md,←⊂''
 md,←⊂''
 md,←⊂'[parm]:linkToCSS   = 1'
 md,←⊂'A paragraph'

 parms←##.MarkAPL.CreateParms
 parms.verbose←0
 ns←##.MarkAPL.Init parms md
 ns←##.MarkAPL.Process ns

 →PassesIf 0=⍴ns.report
 →PassesIf ns.parms.cssURL≡'Firlefanz'
 →PassesIf ns.parms.linkToCSS=0
 →PassesIf 0<+/'<p>[parm]:linkToCSS = 1 A paragraph</p>'⍷∊ns.html

 R←∆OK
