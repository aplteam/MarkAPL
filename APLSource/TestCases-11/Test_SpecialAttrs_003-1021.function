 R←Test_SpecialAttrs_003(stopFlag batchFlag);⎕TRAP;md;ns;expected
⍝ Test {}-syntax for a table: empty special attrbutes
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'|Col A|Col B|  {}'
 md,←⊂'|===|===|'
 md,←⊂'|Row 1/Col A|Row 1/Col B|'
 md,←⊂'|Row 1/Col A|Row 1/Col B|'
 md,←⊂'|Row 1/Col A|Row 1/Col B|'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 expected←'<table>'
 →PassesIf expected≡↑ns.html

 R←∆OK
