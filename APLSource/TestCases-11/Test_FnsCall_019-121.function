 R←Test_FnsCall_019(stopFlag batchFlag);⎕TRAP;md;ns;expected
⍝ Test function call that returns tags - those are converted (it's not an HTML block) while in-line mark-up is done.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 #.⎕SHADOW'TEMP'
 'TEMP'#.⎕NS''
 #.TEMP.MyFns←{'<p>This' '**is** a paragraph.</p>'}

 md←''
 md,←⊂' ⍎⍎#.TEMP.MyFns⍎⍎'    ⍝ The leading blanks is intentional: without it it's an HTML block!!

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 expected←'<p>&lt;p&gt;This</p>' '<p><strong>is</strong> a paragraph.&lt;/p&gt;</p>'
 →PassesIf expected≡ns.html

 R←∆OK
