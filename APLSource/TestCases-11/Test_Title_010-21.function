 R←Test_Title_010(stopFlag batchFlag);⎕TRAP;md;ns;html;title;parms
⍝ Check whether `title` is taken from first (and only) level-1 header) with method `MakeHTML_Doc`.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'Pusemuckel'
 md,←⊂'=========='
 md,←⊂''
 md,←⊂'A paragraph'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns
 parms←##.MarkAPL.CreateParms
⍝parms.title←''   ⍝ NOT set!
 html←parms ##.MarkAPL.MakeHTML_Doc ns.html

 title←{⍵↑⍨⍵⍳'<'}(⍴'<title>')↓{⍵{⍵[1]↑⍵[0]↓⍺}↑'<title>.*</title>'⎕S 0 1⍠('Mode' 'D')⊣⍵}↑,/(⎕UCS 10),¨html
 →PassesIf title≡'Pusemuckel'

 R←∆OK
