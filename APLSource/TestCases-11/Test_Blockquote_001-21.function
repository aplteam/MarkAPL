 R←Test_Blockquote_001(stopFlag batchFlag);⎕TRAP;md;ns;parms
⍝ Test a very simple blockquote.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'> This is a para'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 →PassesIf 1=+/'<blockquote>'⍷∊ns.html
 →PassesIf 1=+/'</blockquote>'⍷∊ns.html
 →PassesIf'<p>This is a para</p>'≡1⊃ns.html

 R←∆OK
