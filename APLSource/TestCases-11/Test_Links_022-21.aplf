 R←Test_Links_022(stopFlag batchFlag);⎕TRAP;ns;html;md;expected
⍝ Test  a link that contains `##` - should become a header. Was once a bug.
⍝ This should only pose a problem when an HTML document that was created by MarkAPL
⍝ is converted back into a Markdown document and then processed again by MarkAPL.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'[## 1. Overview](#overview{#overview .autoheader_anchor})'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 expected←'<div class="h_tag">' '<a href="#1-Overview" id="1-Overview" class="autoheader_anchor">' '<h2>1. Overview</h2>' '</a>' '</div>'
 →PassesIf expected≡ns.html

 R←∆OK
