 R←Test_Table_020(stopFlag batchFlag);⎕TRAP;ns;parms;mat;html
⍝ Test alignment; this was once a bug.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 mat←''
 mat,←⊂'|Name|Remark|No. of users|'
 mat,←⊂'|:---| :-: |---:|'
 mat,←⊂'|APL|Brilliant|800|'

 ns←##.MarkAPL.Init''mat
 ns←##.MarkAPL.Process ns
 html←1↓∊(⎕UCS 13),¨ns.html
 →PassesIf 1=+/'style="text-align: left;"'⍷html
 →PassesIf 2=+/'style="text-align: right;"'⍷html
 →PassesIf 2=+/'style="text-align: center;"'⍷html
 →PassesIf 2=+/'<td '⍷html
 →PassesIf 3=+/'<th '⍷html

 R←∆OK
