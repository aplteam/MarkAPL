 R←Test_InLine_014(stopFlag batchFlag);⎕TRAP;ns;md
⍝ Test escaping `.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 md←''
 md,←⊂'In the first expression the \` is removed...'
 md,←⊂''
 md,←⊂'This `{⍺+⍵}` is APL, this \` is a backtick and this: `¯1⍉vars` is APL again'
 md,←⊂''
 md,←⊂'* In the second expression the \` are removed...'
 md,←⊂'* This `{⍺+⍵}` is APL, this \` is a backtick and this: `¯1⍉vars` is APL again'

 ns←##.MarkAPL.Init''md
 ns←##.MarkAPL.Process ns

 →PassesIf 1=+/'the ` is removed'⍷0⊃ns.html
 →PassesIf 0=+/'<code>'⍷0⊃ns.html

 →PassesIf 1=+/'this ` is '⍷1⊃ns.html
 →PassesIf 2=+/'<code>'⍷1⊃ns.html
 →PassesIf 2=+/'</code>'⍷1⊃ns.html

 →PassesIf 1=+/'the ` are removed'⍷3⊃ns.html
 →PassesIf 0=+/'<code>'⍷3⊃ns.html

 →PassesIf 1=+/'this ` is '⍷4⊃ns.html
 →PassesIf 2=+/'<code>'⍷4⊃ns.html
 →PassesIf 2=+/'</code>'⍷4⊃ns.html

 R←∆OK
